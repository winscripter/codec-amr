This repo is a fork of the 3GPP AMR codec. The only purpose of this fork is
to remove documentation, use latest C language features (e.g. ternary operators),
reduce pointer walking, and reduce number of lines as much as possible.

The motivation is to make this easier to port an AMR codec into object-oriented
languages.
